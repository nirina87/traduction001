{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<div class="main" role="main">
    <section class="section section-no-border pb-0">
        <div class="container py-5">
            <div class="row align-items-end justify-content-between mb-4">
                <div class="col-lg-8">
                    <p class="text-uppercase text-color-primary font-weight-semibold mb-2">Panier</p>
                    <h1 class="font-weight-bold text-8 mb-2">Votre sélection de documents</h1>
                    <p class="text-3 mb-0">Les documents ajoutés au panier sont conservés pendant votre session.</p>
                </div>
                <div class="col-lg-4 text-lg-end mt-3 mt-lg-0">
                    <a href="{{ path('accueil') }}" class="btn btn-outline-primary">Retour à l’accueil</a>
                </div>
            </div>

            {% for message in app.flashes('success') %}
                <div class="alert alert-success mb-4">{{ message }}</div>
            {% endfor %}

            <div class="card border-0 shadow-sm rounded-4 p-4 mb-4">
                <h2 class="h4 mb-3">📎 Envoyer un document à traduire</h2>
                <p class="text-muted mb-3">Choisissez le type de document, la langue cible et ajoutez votre fichier pour obtenir un devis de traduction.</p>
                <form method="post" action="{{ path('panier_ajouter_document') }}" enctype="multipart/form-data" class="row g-3 align-items-end">
                    <div class="col-md-4">
                        <label for="documentId" class="form-label">Document à traduire</label>
                        <select id="documentId" name="documentId" class="form-select" required>
                            <option value="">-- Sélectionner --</option>
                            {% for document in documents %}
                                <option value="{{ document.id }}">{{ document.name }} (base {{ document.basePrice // 100 }}€)</option>
                            {% endfor %}
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="language" class="form-label">Langue cible</label>
                        <select id="language" name="language" class="form-select" required>
                            <option value="">-- Choisir une langue --</option>
                            <option value="Français">Français</option>
                            <option value="Anglais">Anglais</option>
                            <option value="Arabe">Arabe</option>
                            <option value="Espagnol">Espagnol</option>
                            <option value="Italien">Italien</option>
                            <option value="Allemand">Allemand</option>
                            <option value="Portugais">Portugais</option>
                            <option value="Russe">Russe</option>
                            <option value="Chinois">Chinois</option>
                            <option value="Japonais">Japonais</option>
                            <option value="Turc">Turc</option>
                            <option value="Néerlandais">Néerlandais</option>
                            <option value="Suédois">Suédois</option>
                            <option value="Polonais">Polonais</option>
                            <option value="Coréen">Coréen</option>
                            <option value="Hindi">Hindi</option>
                            <option value="Ukrainien">Ukrainien</option>
                            <option value="Persan">Persan</option>
                            <option value="Swahili">Swahili</option>
                        </select>
                    </div>
                    <div class="col-md-3">
                        <label for="documentFile" class="form-label">Votre fichier</label>
                        <input type="file" id="documentFile" name="documentFile" class="form-control" accept=".pdf,.doc,.docx,.rtf,.txt,.jpg,.jpeg,.png" required>
                    </div>
                    <div class="col-md-2">
                        <button type="submit" class="btn btn-primary w-100">Ajouter</button>
                    </div>
                </form>
            </div>

            {% if documents is not empty %}
                <div class="card border-0 shadow-sm rounded-4 p-4 mb-4">
                    <h2 class="h5 mb-3">🌍 Tarifs par document et par langue</h2>
                    <div class="row g-3">
                        {% for document in documents %}
                            <div class="col-md-6 col-xl-4">
                                <article class="border rounded-4 p-3 h-100 bg-light-subtle">
                                    <h3 class="h6 mb-1">{{ document.name }}</h3>
                                    <p class="text-muted small mb-2">{{ document.description|default('Document disponible pour traduction certifiée.') }}</p>
                                    <div class="small text-secondary mb-2">Prix de base : {{ document.basePrice // 100 }} €</div>
                                    {% set rates = translationRates|filter(rate => rate.document.id == document.id) %}
                                    {% if rates|length > 0 %}
                                        <ul class="list-unstyled small mb-0">
                                            {% for rate in rates %}
                                                <li>• {{ rate.language }} : {{ rate.price // 100 }} €</li>
                                            {% endfor %}
                                        </ul>
                                    {% else %}
                                        <p class="small text-muted mb-0">Aucun tarif spécifique n’est encore configuré pour cette langue.</p>
                                    {% endif %}
                                </article>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            {% endif %}

            {% if cart is empty %}
                <div class="card border-0 shadow-sm rounded-4 p-4">
                    <p class="mb-0 text-muted">Votre panier est actuellement vide. Ajoutez un document depuis la page d’accueil ou la page détail.</p>
                </div>
            {% else %}
                <div class="row g-4">
                    {% for item in cart %}
                        <div class="col-md-6 col-xl-4">
                            <article class="card border-0 shadow-sm rounded-4 p-3 h-100">
                                <img src="{{ asset(documents[item.id].image) }}" class="img-fluid rounded-3 mb-3" alt="{{ item.title }}" style="height: 110px; object-fit: cover; width: 100%;" />
                                <h3 class="text-4 mb-2">{{ item.title }}</h3>
                                <p class="text-muted mb-3">{{ item.description }}</p>
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge bg-primary text-light">Quantité : {{ item.quantity }}</span>
                                    <a href="{{ path('produit_detail', {'id': item.id}) }}" class="btn btn-outline-primary btn-sm">Détail</a>
                                </div>
                            </article>
                        </div>
                    {% endfor %}
                </div>

                <div class="d-flex justify-content-end mt-4">
                    <a href="{{ path('commande') }}" class="btn btn-primary btn-lg">Passer la commande</a>
                </div>
            {% endif %}
        </div>
    </section>
</div>
{% endblock %}
